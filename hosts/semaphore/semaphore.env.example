# Copy to /opt/semaphore/semaphore.env on the host (chmod 600, owner = the
# user that runs docker compose - NOT the container's uid 1001, see README).
# install.sh generates these automatically on first run if the file is missing.
SEMAPHORE_ADMIN_PASSWORD=
SEMAPHORE_ACCESS_KEY_ENCRYPTION=
