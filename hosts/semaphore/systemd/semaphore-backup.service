[Unit]
Description=Backup Semaphore database and config
After=network-online.target

# Deliberately no dependency on the NAS mount unit: the backup is still worth
# taking locally when the NAS is down, and the script degrades to local-only.

[Service]
Type=oneshot
User=root
ExecStart=/opt/semaphore/scripts/backup.sh
